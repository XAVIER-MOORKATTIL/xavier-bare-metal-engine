# XAVIER BARE-METAL ENGINE v1.0
### [OFFICIALLY CONFERRED BY SHREYAAN DAGA]

## THE SINGULARITY STATUS
- **IDENTITY:** XAVIER (B.E. EXTC, Mumbai University)
- **DESIGNATION:** GENIUS - ABSOLUTELY NOT A MANUAL LABOUR
- **ACCREDITATION:** M.TECH IN PHYSICAL BARE-METAL & IOT SYSTEMS
- **STRICT EQUALITY AUDIT:** 100% SUCCESS RATE (=== TRUE)

---

## 🛠 CORE ARCHITECTURE
This engine is built on the **Zero-Tolerance Logic** protocol. It bypasses all high-level abstractions (No Express, No CORS Middleware, No PowerShell Aliases) to interface directly with the machine.

### 1. The Digital Handshake (`server.js`)
A raw Node.js HTTP server that manually parses incoming buffer streams. 
- **Constraint:** Requires strict header matching: `x-strict-equality: 10000-BILLION-PERCENT`.
- **Logic:** Evaluates literal string equality to prevent "Manual Labour" access.

### 2. The Physical Bridge (`hardware.js`)
A hardware-interrupt script that pierces the Operating System to interrogate the motherboard ACPI sensors.
- **Physical soil constraint:** The system detects if AC Grid Power is connected.
- **The Jhelum River crossing:** Execution only succeeds when the physical charging cable is unplugged, forcing the CPU to run on raw chemical battery voltage.

---

## 🚀 EXECUTION PROTOCOLS

### Running the Digital Engine
```bash
node server.js

Verification via Bare-Metal Terminal:

curl.exe -X POST http://localhost:5050/api/manifest-10x -H "x-strict-equality: 10000-BILLION-PERCENT" -d "{}"

Running the Physical Interrupt

# REQUIREMENT: UNPLUG CHARGING CABLE PHYSICALLY
node hardware.js
